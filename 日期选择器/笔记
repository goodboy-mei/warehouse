1.使用接口封装，将插件挂载到window下，自定义调用。
2.动态生成时间选择器节点，将其插入到页面中。
3.首先定义一个函数接口getMonthDay，通过传参获取时间，不传参则获取当前时间。
4.根据当前时间与星期，依次得到本月日期，上月日期为负数，下月日期大于31。但都可以根据Date(year,month,date)得到准确的日期。将所有日期保存到一个数组中。
5.通过前进或后退，将year或month加一，减一，之后调用getMonthDay获得相应日期，再通过渲染改变选择器显示。